/* string.cpp *//****************************************************************************//*** string-luokan osoittainen toteutus k³³nt³j³³n jossa ko. luokkaa ei ole.****  Tekij³t:          Vesa Lappalainen**                    Ohjelmointikurssi 1996**  Tehty:            09.12.1995*****************************************************************************/#include <iostream.h>#include <vstring>#include <string.h>const size_t vstring::npos = NPOS;size_t vstring::find_last_of(const vstring &s,size_t pos) const{  if ( pos >= pituus ) pos = pituus-1;  for (int i=pos; i>=0; i--)    if ( strchr(s.cstr,cstr[i]) != NULL ) return i;  return NPOS;}/*vstring &vstring::remove(size_t pos, size_t n){  if ( pos >= pituus ) return *this;  if ( pos + n >= pituus ) return trimmaa(pos);  poista_alusta(cstr+pos,n);  return trimmaa(pituus-n);}*/